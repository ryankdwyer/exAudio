app.factory('VisualizerService', function () {
    //var Visualizer = {};
    //Visualizer.freqData = new Array(100);
    //Visualizer.svgHeight = '300';
    //Visualizer.svgWidth = '1200';
    //Visualizer.barPadding = '1';
    //
    //Visualizer.createSvg = function (parent, height, width) {
    //    return d3.select(parent).append('svg').attr('height', height).attr('width', width);
    //};
    //Visualizer.renderChart = function renderChart(newFreqData, svg) {
    //    var self = this;
    //    requestAnimationFrame(renderChart);
    //
    //    self.freqData = newFreqData;
    //    svg.selectAll('rect')
    //        .data(self.freqData)
    //        .attr('y', function(d) {
    //            return self.svgHeight - d;
    //        })
    //        .attr('height', function(d) {
    //            return d;
    //        })
    //        .attr('fill', function(d) {
    //            return 'rgb(0, 0, ' + d + ')';
    //        });
    //};
    //
    //return Visualizer;
})